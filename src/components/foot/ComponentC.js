export default function ComponentC({year , section }){
    return (
        <div style={{ color: "red", fontWeight: "bold" }}>
        <p>School year: {year}</p>
        <p>Your section: {section}</p>
        </div>
    )
}

